# CoreOps EasyTime connector - administrator PC dependencies.
# Kept deliberately small: this runs on a Windows office PC, not the VPS, and
# shares no code with the CoreOps backend. Versions match backend pins where the
# package is common, so behaviour is identical in both places.
httpx==0.27.2
python-dotenv==1.2.2
# Windows has no system tz database; zoneinfo needs this for Asia/Kolkata.
tzdata==2026.2

# Tests only (the connector's tests are offline - no EasyTime install needed).
pytest==8.3.3
